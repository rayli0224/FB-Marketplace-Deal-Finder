/**
 * Generic shared UI styling constants: layout, typography, colors.
 * Reusable across the app. Bespoke constants live at the top of the component file that uses them.
 */

/** Full-brightness text; use for all on-screen content so nothing appears faded. */
export const CONTENT_TEXT_CLASS = "text-foreground";

/** Monospace content text at xs size (labels, etc.). */
export const CONTENT_TEXT_XS_CLASS = "font-mono text-xs text-foreground";

/** Monospace content text at sm size. */
export const CONTENT_TEXT_SM_CLASS = "font-mono text-sm text-foreground";
